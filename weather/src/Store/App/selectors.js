export const selectUsers = (state) => state.app.users;
export const selectCurrentUser = (state) => state.app.currentUser;
export const selectIsRegistered = (state) => state.app.isRegistered;
export const selectIsLogin = (state) => state.app.isLogin;
export const selectIsEdit = (state) => state.app.isEdit;